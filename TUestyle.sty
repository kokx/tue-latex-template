
\ProvidesPackage{TUestyle}

\usepackage{a4wide}
\usepackage{amsmath,amsthm,amssymb,amsfonts}
\usepackage{mathtools}
\usepackage[english]{babel}
\usepackage{gensymb}
\usepackage{graphicx,wrapfig}
\usepackage{grffile}
\usepackage{pifont}
\usepackage{float}
%\usepackage{subfig}
%\captionsetup[subfloat]{position=bottom}
\usepackage{url}

\usepackage{caption}
\usepackage{subcaption}
\captionsetup{format=plain, labelfont=bf, skip=0pt}

\usepackage{paralist}
\usepackage[usenames,dvipsnames]{color}
\usepackage{moreverb}
\usepackage{lscape}
\usepackage[nottoc]{tocbibind}
\usepackage{verbatim}
\usepackage{array}

\usepackage{algorithm}
\usepackage{algorithmicx}
\usepackage{algpseudocode}

\usepackage{xspace}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{longtable}

\usepackage{xcolor}
\definecolor{darkred}{rgb}{0.5,0,0}
\definecolor{darkgreen}{rgb}{0,0.5,0}
\definecolor{darkblue}{rgb}{0,0,0.5}
\definecolor{gray}{rgb}{0.35,0.35,0.35}

\usepackage[pagebackref=false,
            colorlinks=true,
            linkcolor=black,
            bookmarks=true,
            filecolor=gray,
            urlcolor=gray,
            citecolor=gray
           ]{hyperref}

%\usepackage{titlesec}
%\titleformat{\chapter}{\normalfont\huge\bf}{\thechapter.}{20pt}{\huge\bf}
%\titlespacing*{\chapter}{0pt}{-50pt}{0pt}
%\titlespacing\section{0pt}{12pt plus 4pt minus 2pt}{0pt plus 2pt minus 2pt}
%\titlespacing\subsection{0pt}{12pt plus 4pt minus 2pt}{0pt plus 2pt minus 2pt}
%\titlespacing\subsubsection{0pt}{12pt plus 4pt minus 2pt}{0pt plus 2pt minus 2pt}


% very useful, adds \todo and \missingfigure
\usepackage{todonotes}

% force a \FloatBarrier before the end of the section, keeping all floats near
% its corresponding section
\usepackage[section]{placeins}

\setlength{\parindent}{0.0mm}
\setlength{\parskip}{3mm}
\setlength{\marginparwidth}{2.3cm}

% a nice fancy header and footer (footer only)
\usepackage{fancyhdr}
\fancypagestyle{plain}{
\fancyhf{}
\fancyfoot[LO,RE]{\doctitle}
\fancyfoot[RO,LE]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0.4pt}}

\pagestyle{plain}

% often used mathbb / mathcal shortcuts
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\D}{\mathcal{D}}

\usepackage[backend=biber,style=numeric,citestyle=numeric]{biblatex}

\addbibresource{references.bib}

\graphicspath{{figures/}}


% OPTIONAL GOODIES

% We're not using line numbers
%\usepackage{lineno}
%\modulolinenumbers[5]

% if you want to use indexing, see https://en.wikibooks.org/wiki/LaTeX/Indexing
%\usepackage{makeidx}

% No special text positioning needed. Uncomment if you must do this. Recommend
% against such tomfoolery
\usepackage[absolute]{textpos}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: "main"
%%% End:
